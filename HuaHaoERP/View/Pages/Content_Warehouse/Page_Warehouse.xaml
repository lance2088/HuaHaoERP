<Page x:Class="HuaHaoERP.View.Pages.Content_Warehouse.Page_Warehouse"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" FontFamily="SimSun" TextOptions.TextFormattingMode="Display"
      d:DesignHeight="448" d:DesignWidth="749"
	Title="Page4">

    <Grid>
        <TabControl Margin="0" BorderThickness="0" Padding="0">
            <TabItem Header="库存管理" Template="{DynamicResource TabItemControlTemplate_LevelTwoTab}" Height="25" Margin="7,0,-7,0" Foreground="White">
                <Grid Background="White">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="205*"/>
                        <ColumnDefinition Width="544*"/>
                    </Grid.ColumnDefinitions>
                    <DataGrid Margin="10,40,10,10" BorderThickness="1" Grid.Column="1">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="序号"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <DataGrid Margin="10,40,10,10" BorderThickness="1">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="产品名称"/>
                            <DataGridTextColumn Header="数量"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <DatePicker Grid.Column="1" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="120"/>
                    <Label Content="仓库数量：" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Padding="0,2,5,2" VerticalContentAlignment="Center" Height="24"/>
                    <Button Content="添加" Grid.Column="1" HorizontalAlignment="Right" Margin="0,10,10,0" VerticalAlignment="Top" Width="75" Template="{DynamicResource ButtonControlTemplate_CommonDefault}"/>
                    <ComboBox HorizontalAlignment="Right" Margin="0,10,10,0" VerticalAlignment="Top" Width="70" Height="24" VerticalContentAlignment="Center"/>
                    <ComboBox Grid.Column="1" HorizontalAlignment="Left" Margin="135,10,0,0" VerticalAlignment="Top" Width="120" Height="24" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="全部出入库记录" IsSelected="True"/>
                        <ComboBoxItem Content="入库记录" />
                        <ComboBoxItem Content="出库记录" />
                    </ComboBox>
                </Grid>
            </TabItem>
            <TabItem Header="原料管理" Template="{DynamicResource TabItemControlTemplate_LevelTwoTab}" Margin="17,0,-17,0" Foreground="White">
                <Grid Background="White">
                    <DataGrid Margin="0,40,0,0" BorderThickness="0,1,0,0"/>
                </Grid>
            </TabItem>
            <TabItem Header="余料管理" Template="{DynamicResource TabItemControlTemplate_LevelTwoTab}" Margin="27,0,-27,0" Foreground="White">
                <Grid Background="White">
                    <DataGrid Margin="0,40,0,0" BorderThickness="0,1,0,0"/>
                </Grid>
            </TabItem>
        </TabControl>

    </Grid>
</Page>
